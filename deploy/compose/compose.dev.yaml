services:
  postgres:
    image: postgres:18.6
    environment:
      POSTGRES_DB: naamive
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "54329:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d naamive"]
      interval: 2s
      timeout: 3s
      retries: 30
