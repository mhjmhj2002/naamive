# EX-006 — WI-002 Evidência de Rework Técnico

**execution:** EX-006  
**work_item:** WI-002 — Principal Persistence  
**cause:** CR-WI002-02-F001  
**previous_execution:** EX-005  
**logical_executor_principal:** agent:codex:implementation:WI-002:EX-006  
**captured_at:** 2026-09-13T15:32:04-03:00  
**business_baseline_ref:** PBL-PRJ001-R1-v1.0  
**normative_baseline_ref:** NB-0002  
**governing_decision:** decisions/DEC-008_WI002_PRINCIPAL_SEMANTICS.md  
**result:** REMEDIATION IMPLEMENTED / AWAITING INDEPENDENT RE-REVIEW

## Remediação

- A migration forward-only `000004_authority__principal_expected_version_required`
  usa `CREATE OR REPLACE FUNCTION` apenas para
  `authority.change_principal_username` e `authority.change_principal_status`.
- Ambas executam `IF expected_version IS NULL THEN RAISE EXCEPTION 'Principal
  expected version is required'; END IF;` antes de qualquer leitura, lock ou
  escrita do estado material.
- As funções continuam `SECURITY DEFINER`, com `search_path = pg_catalog,
  authority`; ownership e grants existentes são preservados por replacement e
  nenhum privilégio runtime foi ampliado.

## Provas observadas

```text
pnpm install --frozen-lockfile.......................... PASS
pnpm run typecheck....................................... PASS
pnpm test................................................ PASS
pnpm run architecture.................................... PASS
pnpm run test:architecture............................... PASS
pnpm run build........................................... PASS
PostgreSQL 18.6 clean 000001 → 000004.................... PASS
Principal integration (real PostgreSQL/runtime role)..... PASS
Foundation E2E, temporary port 4174...................... PASS (1 test)
```

The clean PostgreSQL 18.6 integration applied migrations `000001`, `000002`,
`000003` and `000004` successfully. At the actual database command boundary
as `naamive_web`, a NULL username mutation and a NULL status mutation each
rejected with `Principal expected version is required`. For each rejection the
integration compared the complete Principal snapshot and complete ordered
history before and after: username, status, version, current-history pointer,
history count and material history facts were unchanged.

The same integration retained the valid expected-version path (exactly +1,
one material history fact and coherent pointer), stale non-NULL rejection with
no new fact, runtime raw-DML denial, and two independent runtime connections
with one same-version concurrent winner and one version conflict. Catalog
inspection confirmed both controlled functions remain `SECURITY DEFINER` with
the explicit safe search path.

## Environment and process incidents

The first isolated integration attempt used a non-canonical temporary database
name and failed before migrations because the repository bootstrap correctly
targets database `naamive`. The container was removed, then one materially
changed retry used the canonical name and passed. The temporary PostgreSQL
container was removed after validation. Port 4173 was occupied by an unrelated
process, so E2E used configurable temporary port 4174; it was confirmed free
after Playwright cleanup. No orphan process remains.

This evidence is not independent resolution, review, audit or acceptance.
