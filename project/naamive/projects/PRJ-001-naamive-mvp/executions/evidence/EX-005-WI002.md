# EX-005 — WI-002 Evidência de Rework Técnico

**execution:** EX-005  
**work_item:** WI-002 — Principal Persistence  
**cause:** CR-WI002-F001  
**previous_execution:** EX-004  
**logical_executor_principal:** agent:codex:implementation:WI-002:EX-005  
**captured_at:** 2026-09-13T12:20:58-03:00  
**business_baseline_ref:** PBL-PRJ001-R1-v1.0  
**normative_baseline_ref:** NB-0002  
**governing_decision:** decisions/DEC-008_WI002_PRINCIPAL_SEMANTICS.md  
**result:** REMEDIATION IMPLEMENTED / AWAITING INDEPENDENT RE-REVIEW

## Remediação

- A migration forward-only `000003_authority__principal_command_boundary` preserva `000002` histórico e revoga `INSERT`/`UPDATE` em `authority.principal` e `authority.principal_history` para `naamive_web` e `naamive_worker`; os papéis retêm somente `SELECT`.
- As únicas operações de criação e mutação são funções `SECURITY DEFINER` pertencentes ao migrator, com `search_path` explícito `pg_catalog, authority`, referências qualificadas, sem `PUBLIC EXECUTE` e `EXECUTE` mínimo apenas aos dois papéis runtime.
- Os comandos exigem `expectedVersion` nas mutações, bloqueiam a linha atual, rejeitam stale, avançam a versão uma única vez, inserem um único fato material coerente e atualizam snapshot/ponteiro na mesma operação. Criação fixa `ACTIVE`, versão 1 e um único `PRINCIPAL_CREATED`.
- O adapter deixou de compor DML de tabela e chama exclusivamente os comandos controlados. Não houve mudança de semântica de DEC-008 nem escopo WI-003.

## Provas executadas

```text
corepack pnpm install --frozen-lockfile................ PASS
corepack pnpm -r --if-present run typecheck............ PASS
corepack pnpm -r --if-present run test................. PASS
node scripts/architecture-guardrails.mjs................ PASS
node --test scripts/architecture-guardrails.test.mjs.... PASS
corepack pnpm -r --if-present run build................ PASS
PostgreSQL 18.6 clean integration....................... PASS
Foundation E2E, temporary port 4174..................... PASS (1 test)
```

The disposable PostgreSQL 18.6 integration applied `000001`, `000002` and `000003` successfully. It uses `naamive_web` for legitimate creation and mutations, then proves direct runtime `INSERT` into history, direct canonical creation, direct version/pointer update, arbitrary username/status update, and history `UPDATE`/`DELETE` are all rejected with `permission denied`.

The same real integration creates two independent `naamive_web` connections and concurrently submits username changes with the same expected version. It asserts exactly one success, one `PrincipalVersionConflictError`, final version `N + 1`, exactly one new history fact and a matching current-history pointer. It also retains the stale no-op/no-fact proof from the normal Principal flow.

The root package scripts rely on a global `pnpm` shim unavailable in this runtime; equivalent commands were run directly through pinned Corepack pnpm 12.3.4. The temporary E2E configuration used port 4174 and did not alter the unrelated service on port 4173. No limitation remains for the required technical result; this evidence is not independent resolution, review, audit or acceptance.
