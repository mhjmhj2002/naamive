# Evidência — EX-007 / WI-003

**execution_id:** EX-007  
**work_item:** WI-003  
**principal:** agent:codex:implementation:WI-003  
**authority_ref:** ../../governance/AUTH-WI003-EXECUTION-01.md  
**business_baseline_ref:** PBL-PRJ001-R1-v1.0  
**normative_baseline_ref:** NB-0002  
**collected_at:** 2026-09-14T20:09:01-03:00

## Trabalho e artefatos

- `database/migrations/000005_security__password_login.ts`: Credential durável
  vinculada por `principal_id`; sinais e falhas persistentes de rate limit;
  funções de decisão atômicas e ordem de lock `IP → USERNAME_IP`.
- `packages/modules/src/security/login.ts`: validação, Argon2id (64 MiB, 3
  iterações, paralelismo 1, salt 16 bytes, hash 32 bytes), resultado interno
  `AuthenticatedPrincipal` e antienumeração.
- `packages/database/src/login.ts`: adapter PostgreSQL; `apps/web/src/server/app.ts`:
  boundary Fastify/TypeBox, respostas públicas e semântica de proxy confiável.

## Testes e resultados

| Check | Resultado |
|---|---|
| `pnpm --filter @naamive/modules test` | PASS — Argon2id, sucesso efêmero, falha genérica, atraso e limite. |
| `pnpm --filter @naamive/web test` | PASS — 8 testes; 204 sem portador reutilizável, 400, 401, 429 e proxy confiável. |
| `pnpm typecheck` | PASS — todos os packages. |
| `DATABASE_URL=… timeout 180s pnpm db:integration` | PASS em PostgreSQL 18.6 descartável; migrações, Credential, restart e concorrência. |

A primeira integração revelou que o migrador não possuía `CREATE` no schema
`security`; a correção limitada em `packages/database/src/bootstrap.ts` foi
aplicada e a segunda tentativa, em banco descartável, passou. O container
`naamive-wi003-pg-test` foi encerrado após o check.

## Evidência negativa e limites

- Username inexistente, Principal não ativo, Credential ausente/revogada e
  password inválida usam o mesmo `401 AUTHENTICATION_FAILED` público; nenhum
  hash, salt, `principal_id` ou causa interna é exposto.
- Request estrutural inválido não consulta Credential nem conta tentativa e
  retorna somente `400 VALIDATION_ERROR`.
- Não foi criado endpoint de provisionamento/administração de senha: essa
  authority permanece fora do escopo de WI-003. A fixture de integração grava
  Credential apenas como dado de teste sob o banco descartável.
- O ambiente disponível executou os checks em Node `v24.18.1`, abaixo do pin
  `24.21.0` do TIR. O comportamento foi validado, mas o review deve exigir a
  repetição no runtime pinado antes de uma decisão de aceite.
- Não há finding bloqueador conhecido. Review independente e auditoria/decisão
  de aceite continuam pendentes; esta evidência não aceita WI-003.
