services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: naamive
      POSTGRES_PASSWORD: naamive
      POSTGRES_DB: naamive
    ports: ["127.0.0.1:5432:5432"]
    volumes: ["naamive-postgres:/var/lib/postgresql/data"]
volumes:
  naamive-postgres:
